<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pomodoro Navi - プロフィール</title>
    <%= stylesheet_link_tag 'top', media: 'all' %>
    <%= stylesheet_link_tag 'formpage', media: 'all' %>
</head>
  <body>
    <main>
      <div class="formpage-title">Profile</div><br>

      <%= form_with(model: resource, url: registration_path(resource_name), local: true, method: :put) do |f| %>
          <%= f.label :name, "お名前" %>
          <div class="formpage-field">
            <%= f.text_field :name, autocomplete: "name", placeholder: "お名前" %>
          </div>

          <%= f.label :email, "メールアドレス" %>
          <div class="formpage-field">
            <%= f.email_field :email, autocomplete: "email", placeholder: "メールアドレス" %>
          </div>

          <%= f.label :profile, "プロフィール（任意）" %>
          <div class="formpage-textarea">
            <%= f.text_area :profile, autocomplete: "profile", placeholder: "今取り組んでいることや目標を設定しましょう。ナビが応援してくれます。", rows: 6 %>
          </div>

          <div class="formpage-actions">
            <%= f.submit "プロフィールを更新" %>
          </div><br>
        <% end %>

        <div class="error_message"><%= render 'shared/error_messages', model: resource %></div>
    </main>
  </body>
</html>