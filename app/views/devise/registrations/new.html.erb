<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up - Pomodoro Navi</title>
    <%= stylesheet_link_tag 'top', media: 'all', 'data-turbolinks-track': 'reload' %>
</head>
<body>
    <main>
        <h1>Signup</h1><br>

        <%= form_with(model: resource, url: registration_path(resource_name), local: true) do |f| %>
          <div class="field">
            <%= f.text_field :name, autocomplete: "name", placeholder: "お名前" %>
          </div><br>

          <div class="field">
            <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "メールアドレス" %>
          </div><br>

          <div class="field">
            <%= f.password_field :password, autocomplete: "new-password", placeholder: "パスワード" %>
          </div><br>

          <div class="field">
            <%= f.password_field :password_confirmation, autocomplete: "new-password", placeholder: "もう一度同じパスワードをご入力ください" %>
          </div><br>

          <div class="actions">
            <%= f.submit "新規登録" %>
          </div>
        <% end %><br>

        <%= render 'shared/error_messages', model: resource %>
        <%= render "devise/shared/links" %>
    </main>
</body>
</html>